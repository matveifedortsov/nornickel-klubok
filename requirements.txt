# --- Core (нужно везде) ---
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
numpy>=1.26

# --- Парсинг PDF/DOCX/PPTX/таблиц ---
pymupdf>=1.24          # быстрый парс текста/структуры (PDF)
pdfplumber>=0.11       # таблицы (PDF)
python-docx>=1.1       # внутренние статьи/обзоры (DOCX)
python-pptx>=0.6.23    # доклады/презентации (PPTX)
# grobid-client-python>=0.0.8  # опционально, если поднимете GROBID

# --- Хранилища ---
neo4j>=5.19            # граф
qdrant-client>=1.12    # вектор (нужен query_points API; .search убран в 1.10)

# --- Локальный эмбеддер (ONNX, без torch/квоты) ---
fastembed>=0.3         # мультиязычный локальный эмбеддер; работает там, где torch падает

# --- Эмбеддинги ---
# Боевой бэкенд (Yandex AI Studio) эмбеддит по HTTP и НЕ требует torch.
# Тяжёлый локальный BGE-m3 (sentence-transformers→torch, ~2-3 ГБ) вынесен в
# requirements-ml.txt — ставьте его ТОЛЬКО при EMBEDDER_BACKEND=bge:
#     pip install -r requirements-ml.txt
# Так основной образ/окружение остаётся лёгким.

# --- API / UI ---
fastapi>=0.110
uvicorn[standard]>=0.29
streamlit>=1.34
requests>=2.31
reportlab>=4.1          # экспорт ответов в PDF
streamlit-agraph>=0.0.45  # визуализация подграфа

# --- Утилиты ---
pint>=0.23             # нормализация единиц измерения
tqdm>=4.66

# --- Тесты ---
pytest>=8.0
