# --- Core (нужно везде) ---
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
numpy>=1.26

# --- Парсинг PDF/DOCX/PPTX/таблиц ---
pymupdf>=1.24          # быстрый парс текста/структуры (PDF)
pdfplumber>=0.11       # таблицы (PDF)
python-docx>=1.1       # внутренние статьи/обзоры (DOCX)
python-pptx>=0.6.23    # доклады/презентации (PPTX)
# grobid-client-python>=0.0.8  # опционально, если поднимете GROBID

# --- Хранилища ---
neo4j>=5.19            # граф
qdrant-client>=1.9     # вектор

# --- Эмбеддинги / реранк (тяжёлое — ставится когда есть железо) ---
sentence-transformers>=2.7   # BGE-m3 / e5
# FlagEmbedding>=1.2          # альтернативный загрузчик BGE

# --- LLM-инференс (ставится на машине с GPU) ---
# vllm>=0.4                   # рекомендуется для MetalGPT-1 (32B)
# transformers>=4.40
# accelerate>=0.30
# bitsandbytes>=0.43          # 4-bit квантизация

# --- API / UI ---
fastapi>=0.110
uvicorn[standard]>=0.29
streamlit>=1.34
requests>=2.31
reportlab>=4.1          # экспорт ответов в PDF
streamlit-agraph>=0.0.45  # визуализация подграфа

# --- Утилиты ---
pint>=0.23             # нормализация единиц измерения
tqdm>=4.66

# --- Тесты ---
pytest>=8.0
